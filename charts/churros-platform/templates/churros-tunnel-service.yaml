apiVersion: v1
kind: Service
metadata:
  name: churros-tunnel-proxy
spec:
  ports:
    - name: http
      protocol: TCP      
      port: 8080
      targetPort: 8080
  selector:
    app: churros-tunnel
  type: ClusterIP