apiVersion: v1
kind: Service
metadata:
  name: churros-api
spec:
  ports:
    - name: http
      protocol: TCP      
      port: 8000
      targetPort: 8000
  selector:
    app: churros-api
  type: ClusterIP